# Phase 2 TSR Evaluation Results

**Metrics deferred** — grid-level golden GT not yet annotated.

Targets (when GT available): cell_accuracy >= 95%, merged_cell_accuracy >= 90%

## Overall: METRICS DEFERRED

Run on GPU lab with `vlm_enabled: true` in `configs/default.yaml`:

```bash
python eval/phase2/run_eval.py --corpus corpus/golden --save-outputs --report eval/phase2/PHASE2_RESULTS.md
```
